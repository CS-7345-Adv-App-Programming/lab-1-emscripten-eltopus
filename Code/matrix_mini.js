class JSMatrix{multMatrix(t){let r=[],a=[],o=[];for(let e=0;e<t;e++)r.push([Math.random().toFixed(5),Math.random().toFixed(5)]),a.push([Math.random().toFixed(5),Math.random().toFixed(5)]),o.push([Math.random().toFixed(5),Math.random().toFixed(5)]);const e=performance.now();return this.multiplyMatrices(r,a),performance.now()-e}multiplyMatrices(t,r){for(var a=[],o=0;o<t.length;o++){a[o]=[];for(var e=0;e<r[0].length;e++){for(var n=0,h=0;h<t[0].length;h++)n+=t[o][h]*r[h][e];a[o][e]=n}}return a}}